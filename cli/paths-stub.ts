// TEMPORARY STUB — replaced in Task 16 by cli/version.ts (which reads from package.json).
// Remaining only because cli/self-update.ts still imports VERSION.
// Will be deleted at Task 16.
export const VERSION = "0.1.0";
